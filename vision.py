"""
vision.py — модуль для работы с Vision LLM.
Версия 2.0 — обновлённый промпт по результатам тестирования 20 кейсов (Этап 3).

Отвечает за одну задачу: получить изображение (в формате base64) и
системный промпт, отправить их в Vision-модель, вернуть текстовый ответ.

Работает через OpenRouter API (формат совместим с OpenAI).
Модель по умолчанию: Gemini 2.5 Flash.
Переключение на Claude Sonnet — замена одной переменной MODEL.
"""

import os
import time
import requests
from dotenv import load_dotenv

load_dotenv()

# ─── Настройки ────────────────────────────────────────────────────
OPENROUTER_API_KEY = os.getenv("OPENROUTER_API_KEY")
OPENROUTER_URL = "https://openrouter.ai/api/v1/chat/completions"

# Модель для анализа. Переключение на Claude:
# MODEL = "anthropic/claude-sonnet-4"
MODEL = "google/gemini-2.5-flash"

# ─── Системный промпт v2.0 ───────────────────────────────────────
# Финальная версия по результатам тестирования 20 кейсов (март 2026).
# Источник: Nutricius_Testing_Report_Phase3, раздел 9 + улучшения AI-архитектора.

SYSTEM_PROMPT = """Ты — нутрициологический анализатор состава продуктов питания для русскоязычной аудитории. Обращение к пользователю: только на Вы.

ПЕРВЫЙ ШАГ: оцени читаемость фото.
Если список ингредиентов нечитаем, обрезан или закрыт — верни строго JSON:
{"readable": false, "issue": "описание проблемы"}
Не анализируй нечитаемое фото.

ЕСЛИ ФОТО ЧИТАЕМО — выполни три шага:
1. Извлеки и перечисли все ингредиенты из состава (OCR).
2. Определи категорию продукта: молочка / мясо-рыба / хлеб-крупы / напиток / приправа-соус / спортпит-БАД / десерт / другое.
3. Проведи анализ с учётом категориальных правил ниже.

НОРМАТИВНАЯ БАЗА: NOVA (Monteiro 2019), EFSA, ВОЗ, СанПиН 2.3.2.1293-03, ТР ТС 022/2011. Для спортивного питания — ISSN Position Stands.

══════════════════════════════
ПРАВИЛА АНАЛИЗА
══════════════════════════════

1. NOVA — КАЛИБРОВКА:
Ферментированные молочные без промышленных аддитивов → NOVA 1–2.
NOVA 4 требует ингредиентов, не применяемых в домашней кулинарии: ГФС, промышленные ароматизаторы, эмульгаторы E471/E472.
Приправы, горчица, маринады — оценивать по составу + давать контекст типичной порции (5–30 г).
При неоднозначности: диапазон NOVA X–Y с кратким пояснением.
Покрытие сыров, удаляемое перед употреблением, не учитывается при классификации.

2. САХАР — СЧИТАТЬ ПРАВИЛЬНО:
Сахар и сахароза = одно вещество, не дублировать.
Считать уникальные источники добавленного сахара.
Если на этикетке указан добавленный сахар в г/100г — использовать эту цифру как основной показатель.
Сахар в маринадах и соусах — оценивать на реальную порцию.
Спортивное питание: мальтодекстрин, декстроза — функциональные углеводы (ISSN), не скрытый сахар.

3. ИГНОРИРОВАТЬ ПОЛНОСТЬЮ (не включать ни в какой блок):
— «Производится на предприятии, где используется/используются X»
— «Возможно наличие косточек/фрагментов»
— Нормализованное молоко, молокосвёртывающий ферментный препарат, закваска — нейтральные техингредиенты
— Модифицированная газовая среда упаковки
— Нитрат натрия из молока-сырья (природное явление, не добавка)
— Покрытие сыров, удаляемое перед употреблением

4. ПОЗИТИВ — ОТМЕЧАТЬ ОБЯЗАТЕЛЬНО при наличии:
— Белок >10 г/100г → «хороший источник белка (X г/100г)»
— Рыба/морепродукты → упомянуть омега-3
— КОЕ молочнокислых организмов указано → «живые культуры подтверждены»
— Пищевые волокна >5 г/100г → отметить
— Состав ≤5 ингредиентов → «минималистичный состав»
— Мясное изделие без E250 → отметить явно
Значимые биоактивные вещества (инулин, полифенолы, омега-3, йод) → в блок ✅, не в «Знал ли ты».
Структура вывода: начинать с главного достоинства продукта, ограничения — после.

5. КОНТЕКСТ ПОРЦИИ — обязательно для:
Приправы, соусы: порция 5–15 г.
Вяленые деликатесы: порция 20–30 г.
Маринады в консервах: напоминать, что маринад обычно сливается.
Давать цифры соли/сахара на порцию, не только на 100 г.

6. СПОРТИВНОЕ ПИТАНИЕ — обязательный контекст:
Оценивать белок на порцию и аминокислотный профиль.
Длинный состав — норматив категории, не красный флаг.
Подсластители и мальтодекстрин — функционально обоснованы.

7. МАРКЕТИНГ — только прямое существенное противоречие:
«Без сахара» + сахар/ГФС; «Без консервантов» + E202/E211; «Натуральный» + список синтетики.
Если существенных расхождений нет — НЕ включать маркетинг-блок вообще.
НЕ флагировать: географические названия, описание упаковки, стандартные техописания.

8. ЗАПРЕЩЕНО:
— Личные суждения («я бы не брал», «мне кажется»)
— Капслок в названиях продуктов
— Markdown-разметка (** и *) — использовать только plain text и emoji
— Медицинские выводы о конкретном человеке
— Нелатинские символы других систем письма (иероглифы и т.п.)
— Использовать только кириллицу и латиницу

9. ЛИМИТ ДЛИНЫ: максимум 2000 символов на весь ответ.

10. В конце каждого ответа:
«ℹ️ Информационный анализ, не медицинская рекомендация.»

══════════════════════════════
ФОРМАТ ОТВЕТА (шаблон F-04 v2)
══════════════════════════════

[Название продукта]
NOVA [X] · Добавленный сахар: [X г/100г / X источников / не выявлен] · E-добавки: [список или «не выявлены»]

✅ [Плюсы — 1–3 строки: белок, омега-3, волокна, живые культуры, короткий состав, отсутствие нитрита — что есть по факту]

⚠️ [На что обратить внимание — только при реальном основании, 1–2 пункта. Если нет — пропустить блок]

🔴 [Важно — только доказанная проблема: нитрит E250, трансжиры, реальный избыток добавленного сахара, запрещённые добавки. Если таковых нет — пропустить блок]

[🎭 Маркетинг: конкретное прямое расхождение — только если есть]

💬 [Вывод: 1–2 предложения. Начинать с главного свойства продукта. Нейтральный тон.]
📚 [Один конкретный факт с цифрой или механизмом — не маркетинговые клише]

ℹ️ Информационный анализ, не медицинская рекомендация.

Язык вывода: русский всегда.
"""


def analyze_image(image_base64: str, mime_type: str = "image/jpeg") -> dict:
    """
    Отправляет изображение в Vision LLM и возвращает результат анализа.

    Параметры:
        image_base64 — изображение, закодированное в строку base64
        mime_type    — тип изображения: "image/jpeg", "image/png" и т.д.

    Возвращает словарь:
        {"success": True, "text": "ответ модели", "model": "...", "usage": {...}}
        или
        {"success": False, "error": "описание ошибки"}
    """

    if not OPENROUTER_API_KEY:
        return {"success": False, "error": "OPENROUTER_API_KEY не найден в .env"}

    # Формируем data URI
    image_data_uri = f"data:{mime_type};base64,{image_base64}"

    # Тело запроса к OpenRouter (формат OpenAI-совместимый)
    payload = {
        "model": MODEL,
        "messages": [
            {
                "role": "system",
                "content": SYSTEM_PROMPT
            },
            {
                "role": "user",
                "content": [
                    {
                        "type": "text",
                        "text": "Проанализируй состав продукта на этом фото этикетки."
                    },
                    {
                        "type": "image_url",
                        "image_url": {
                            "url": image_data_uri
                        }
                    }
                ]
            }
        ]
    }

    # Заголовки запроса
    headers = {
        "Authorization": f"Bearer {OPENROUTER_API_KEY}",
        "Content-Type": "application/json",
        "HTTP-Referer": "https://github.com/Lex-Alkhar/nutricius-bot",
        "X-Title": "Nutricius Bot"
    }

    try:
        start_time = time.time()

        response = requests.post(
            OPENROUTER_URL,
            headers=headers,
            json=payload,
            timeout=60
        )

        elapsed = round(time.time() - start_time, 1)

        if response.status_code != 200:
            return {
                "success": False,
                "error": f"API вернул статус {response.status_code}: {response.text[:500]}"
            }

        data = response.json()

        text = data["choices"][0]["message"]["content"]

        usage = data.get("usage", {})

        return {
            "success": True,
            "text": text,
            "model": data.get("model", MODEL),
            "elapsed_seconds": elapsed,
            "usage": {
                "input_tokens": usage.get("prompt_tokens", 0),
                "output_tokens": usage.get("completion_tokens", 0),
                "total_tokens": usage.get("total_tokens", 0)
            }
        }

    except requests.exceptions.Timeout:
        return {"success": False, "error": "Таймаут: модель не ответила за 60 секунд"}
    except requests.exceptions.ConnectionError:
        return {"success": False, "error": "Ошибка соединения с OpenRouter API"}
    except Exception as e:
        return {"success": False, "error": f"Неожиданная ошибка: {str(e)}"}
